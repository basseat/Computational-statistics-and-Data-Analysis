## Exercise Sheet 1 – Sampling, Variance Estimation, and Poisson Distributions

Generates samples from a normal distribution and computes sample variance via two methods (manual and NumPy). Explores Poisson distributions across varying mu values and visualises the results as overlaid histograms.

**Libraries:** NumPy, Matplotlib
